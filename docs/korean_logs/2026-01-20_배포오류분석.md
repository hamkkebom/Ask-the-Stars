# 🔍 Vercel 배포 오류 분석 보고서

> **작성일**: 2026-01-20
> **배포 시간**: 13:11:05 ~ 13:13:13 (약 2분)
> **결과**: ❌ 404 NOT_FOUND

---

## 📋 요약

| 항목 | 상태 |
|------|:----:|
| 빌드 | ✅ 성공 |
| 배포 | ✅ 완료 |
| 웹사이트 접속 | ❌ 404 오류 |

**빌드와 배포는 성공했으나, 도메인 연결 문제로 404 발생**

---

## 🔍 발견된 문제

### 1. ❌ 배포된 커밋이 다름

```
배포된 커밋: 3cbc29f
최신 커밋: 76abfd7 (방금 푸시한 것)
```

**이유**: Vercel이 캐시 없이 이전 커밋으로 배포했거나, 최신 푸시가 반영되지 않음

---

### 2. ⚠️ Canvas 라이브러리 설치 실패 (경고)

```
canvas@2.11.2/node_modules/canvas install: gyp ERR! configure error
Package 'pixman-1', required by 'virtual:world', not found
```

**영향**: PDF/이미지 처리 기능에 영향 가능 (핵심 기능 아님)
**조치**: 선택적 의존성이므로 빌드 자체는 성공

---

### 3. ⚠️ Prisma 스키마 경로 경고

```
We could not find your Prisma schema in the default locations
```

**영향**: 없음 (packages/database/prisma/schema.prisma 사용 중)

---

## ✅ 정상 작동 확인

| 단계 | 상태 | 시간 |
|------|:----:|------|
| 의존성 설치 | ✅ | 40.2s |
| Prisma Client 생성 | ✅ | 215ms |
| Next.js 컴파일 | ✅ | 34.9s |
| 정적 페이지 생성 (67개) | ✅ | ~3s |
| 배포 완료 | ✅ | 13:13:12 |

---

## 🚨 404 오류 원인 분석

### 가설 1: 도메인-프로젝트 연결 문제

Vercel DNS에서 도메인은 설정되어 있지만, **프로젝트 내 Domains 설정**이 다를 수 있음

**확인 방법**:
1. Vercel Dashboard → `ask-the-stars` 프로젝트
2. Settings → Domains
3. `hamkkebom.com`과 `www.hamkkebom.com`이 **이 프로젝트**에 연결되어 있는지 확인

### 가설 2: 최신 커밋 미반영

로그에 `Commit: 3cbc29f`로 표시 → 우리가 푸시한 `76abfd7`가 아님

**확인 방법**:
1. Vercel Dashboard → Deployments
2. 최신 배포의 Git Commit 확인
3. `76abfd7`가 아니면 다시 배포 필요

### 가설 3: Vercel 기본 URL만 작동

배포는 성공했지만, 커스텀 도메인이 아닌 **Vercel 기본 URL**로만 접속 가능할 수 있음

**확인 방법**:
- `https://ask-the-stars-kappa.vercel.app` 접속 테스트

---

## 📋 즉시 확인 필요 항목

1. [ ] `ask-the-stars-kappa.vercel.app` 접속 테스트
2. [ ] Vercel 프로젝트 > Settings > Domains에서 `hamkkebom.com` 연결 상태 확인
3. [ ] 최신 Deployments에서 Git Commit이 `76abfd7`인지 확인
4. [ ] 아니라면 Redeploy 실행

---

## 🔧 권장 조치

### 즉시

```bash
# Vercel 기본 URL 테스트
curl -I https://ask-the-stars-kappa.vercel.app
```

### 도메인 미연결 시

1. Vercel Dashboard → `ask-the-stars` 프로젝트
2. Settings → Domains → Add
3. `hamkkebom.com` 추가

### 재배포 필요 시

1. Vercel Dashboard → Deployments
2. 최신 배포 옆 ⋮ 클릭 → Redeploy
3. 또는: `vercel --prod` 실행

---


**작성자**: AI Assistant
**검토 필요**: 인프라 담당자

---

## ✅ 최종 Vercel 배포 설정 (Monorepo 이슈 해결) - 2026-01-20 13:51 추가

Next.js 모노레포 구조(`apps/web`)를 Vercel이 올바르게 인식하고 의존성(`pnpm-lock.yaml`)을 정상적으로 설치하기 위한 **필수 설정값**입니다.

### 1. Framework & Root Directory
- **Framework Preset**: `Next.js`
- **Root Directory**: `apps/web`
  - [x] **Include files outside of the root directory in the Build Step** (체크 필수)

### 2. Build & Development Settings (Override)

| 항목 | 설정값 (명령어) | 비고 |
|------|----------------|------|
| **Build Command** | `cd ../.. && pnpm --filter @ask-the-stars/database db:generate && pnpm --filter @ask-the-stars/web build` | 루트로 이동 후 DB 생성 및 웹 빌드 |
| **Output Directory** | `.next` (또는 Override OFF) | Root가 `apps/web`이므로 기본값 사용 |
| **Install Command** | `cd ../.. && pnpm install` | **핵심**: 루트로 이동하여 pnpm 워크스페이스 의존성 설치 |
| **Development Command** | `next dev` (기본값) | 수정 불필요 |

### 💡 설정 원리
1. **Root Directory (`apps/web`)**: 
   - Vercel이 `next.config.js`를 찾고 Next.js 프레임워크 기능을 활성화하기 위해 필수입니다.
2. **Install Command (`cd ../.. && pnpm install`)**: 
   - Root Directory를 하위 폴더로 지정하면, Vercel이 해당 폴더에서 `npm install`을 시도합니다.
   - 하지만 모노레포 설정(`workspace:`)은 최상위 `pnpm-lock.yaml`에 있으므로, `npm` 사용 시 에러가 발생합니다.
   - 따라서 강제로 상위(루트) 폴더로 이동하여 `pnpm`을 사용하도록 지정해야 합니다.
